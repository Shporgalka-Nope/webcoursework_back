# About

## This is only the backend part for [this](https://github.com/Shporgalka-Nope/webcoursework_front.git) frontend

A simple backend that utilises Socket IO library to serve chat web app

- Made via express.js
- Uses Sequelize
- Uses TypeScript

## Please note: sequelize component file requires you to change the config to your database, or just install sqlite3 package
